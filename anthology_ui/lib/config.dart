import 'package:flutter/material.dart';

const expandedWidth = 840;
const screenMainScrollViewHorizontalPadding = EdgeInsets.symmetric(
  horizontal: 16.0,
);

class SharedPrefsKeys {
  static const useHttpGateway = 'use_http_gateway';
  static const localDataFolder = 'local_data_folder';
}
